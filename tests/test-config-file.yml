name: Test config_file input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          directory: ./actions/hardened/bridgecrewio--checkov-action/v12.1347.0/tests/fixtures/terraform
          config_file: ./actions/hardened/bridgecrewio--checkov-action/v12.1347.0/tests/fixtures/checkov-config/.checkov.yaml
          log_level: WARNING
      - name: Assert action completed
        run: |
          echo "PASS: action completed with config_file input"
